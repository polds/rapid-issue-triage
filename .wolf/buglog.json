{
  "version": 1,
  "bugs": [
    {
      "id": "bug-001",
      "timestamp": "2026-08-27T19:20:00-07:00",
      "error_message": "Datadog showed 'set Datadog keys in Settings' but the key inputs were hidden until the source toggle was on",
      "file": "web/src/pages/Settings.tsx",
      "root_cause": "Secret rows were gated on src.enabled, so unavailable sources had no way to set keys before enabling",
      "fix": "Always render secret fields for sources that declare them, independent of the enabled toggle",
      "tags": [
        "settings",
        "ux",
        "secrets"
      ],
      "related_bugs": [],
      "occurrences": 1,
      "last_seen": "2026-08-27T19:20:00-07:00"
    },
    {
      "id": "bug-002",
      "timestamp": "2026-08-28T10:40:00-07:00",
      "error_message": "Release workflow run succeeded and packaged archives/SBOMs but created no GitHub Release",
      "file": ".github/workflows/release.yml",
      "root_cause": "The only Release run was a workflow_dispatch on main, so github.ref_type was 'branch'. The args expression selected 'release --snapshot --clean', which builds and packages into dist/ but never publishes; the attestation step was skipped by the same condition. There was no way for a manual run to publish.",
      "fix": "Added an optional workflow_dispatch 'tag' input. A job-level RELEASE_TAG env resolves to github.ref_name on a v* tag push or the dispatch input otherwise; when non-empty the job checks out that tag, runs 'release --clean' with GORELEASER_CURRENT_TAG, and attests provenance. Empty still means a snapshot dry run, now uploading dist as a 'snapshot-dist' artifact.",
      "tags": [
        "ci",
        "release",
        "goreleaser",
        "github-actions",
        "workflow_dispatch"
      ],
      "related_bugs": [],
      "occurrences": 1,
      "last_seen": "2026-08-28T10:40:00-07:00"
    },
    {
      "id": "bug-003",
      "timestamp": "2026-08-28T11:00:00-07:00",
      "error_message": "Could not push tag v0.1.0: 'error: RPC failed; HTTP 403' on git-receive-pack for refs/tags/*",
      "file": ".github/workflows/release.yml",
      "root_cause": "A Claude Code remote session's git credentials permit branch pushes (claude/*) but refuse refs/tags/*. Separately, the obvious workaround \u2014 creating the release in the GitHub Releases UI \u2014 is unsafe with immutable releases enabled: it publishes instantly, so GoReleaser cannot attach assets and the tag name is permanently burnt.",
      "fix": "Added a create_tag checkbox to the Release workflow's workflow_dispatch so the job mints the tag and releases it in one run. Added replace_existing_draft so a mid-upload failure can be retried, and tightened tag validation to a real semver regex since a bad tag would otherwise be pushed before GoReleaser rejects it.",
      "tags": [
        "ci",
        "release",
        "goreleaser",
        "immutable-releases",
        "git",
        "403",
        "tags"
      ],
      "related_bugs": [
        "bug-002"
      ],
      "occurrences": 1,
      "last_seen": "2026-08-28T11:00:00-07:00"
    },
    {
      "id": "bug-004",
      "timestamp": "2026-08-28T11:10:00-07:00",
      "error_message": "release failed after 6m2s - failed to upload triage_0.1.0_linux_i386.rpm.spdx.json: 422 Validation Failed [{Resource:ReleaseAsset Field:name Code:already_exists}]",
      "file": ".goreleaser.yaml",
      "root_cause": "release.extra_files globbed dist/*.spdx.json, but GoReleaser already uploads every SBOM-typed artifact to the release. All 42 SBOMs were therefore queued twice and the duplicate POST returned 422 already_exists. Snapshot runs never upload, so 4 green snapshot runs could not catch it - it only surfaced on the first real tagged release.",
      "fix": "Removed release.extra_files. checksum.extra_files stays: it only adds the SBOMs as checksums.txt subjects (so provenance covers them) and uploads nothing.",
      "tags": [
        "release",
        "goreleaser",
        "sbom",
        "422",
        "duplicate-upload",
        "immutable-releases"
      ],
      "related_bugs": [
        "bug-002"
      ],
      "occurrences": 1,
      "last_seen": "2026-08-28T11:10:00-07:00"
    },
    {
      "id": "bug-005",
      "timestamp": "2026-08-28T11:16:00-07:00",
      "error_message": "Re-running the Release workflow for existing tag v0.1.0 would have reproduced the identical '422 already_exists' SBOM failure even though the fix was already merged to main (caught from the run's head_sha and cancelled before it built)",
      "file": ".github/workflows/release.yml",
      "root_cause": "Publishing an already-existing tag checks out THAT TAG (ref: env.RELEASE_TAG), and GoReleaser reads .goreleaser.yaml from the checked-out tree. Tag v0.1.0 pointed at ff9f062, which predates the PR #13 fix, so the tag's tree still carried the broken release.extra_files glob. A fix merged to main is invisible to a release cut from an older tag. Only the workflow YAML itself comes from the dispatched ref; all repository content comes from the checkout.",
      "fix": "Cut v0.1.1 from main with create_tag=true instead. That path checks out main (which has the fix) and creates the tag there, so GoReleaser reads the corrected config. A tag whose tree contains a broken release config cannot be repaired by fixing main - only by deleting and re-creating the tag, or by moving to a new version number.",
      "tags": [
        "release",
        "goreleaser",
        "github-actions",
        "tags",
        "checkout",
        "frozen-config",
        "immutable-releases"
      ],
      "related_bugs": [
        "bug-004"
      ],
      "occurrences": 1,
      "last_seen": "2026-08-28T11:16:00-07:00"
    },
    {
      "id": "bug-006",
      "timestamp": "2026-08-28T18:45:00-07:00",
      "error_message": "`go test ./...` listed github.com/polds/rapid-issue-triage/web/node_modules/flatted/golang/pkg/flatted after adding ESLint",
      "file": "Makefile",
      "root_cause": "eslint -> file-entry-cache -> flat-cache -> flatted, and flatted vendors a Go implementation (flatted.go). Go's `./...` pattern only skips dirs named testdata or starting with . or _, so web/node_modules is walked. It is gitignored, so this only bites after a local `npm install` - CI never installs npm in the Go jobs, which is why CI stayed green.",
      "fix": "Added GO_PKGS (go list ./... filtered through grep -v /node_modules/) and GO_FILES (git ls-files '*.go') to the Makefile and used them in test/test-race/vet/fix-check/fmt-check/vuln. Also added web/node_modules to both exclusions.paths blocks in .golangci.yml.",
      "tags": [
        "go",
        "makefile",
        "node_modules",
        "eslint",
        "tooling",
        "golangci-lint"
      ],
      "related_bugs": [],
      "occurrences": 1,
      "last_seen": "2026-08-28T18:45:00-07:00"
    },
    {
      "id": "bug-007",
      "timestamp": "2026-08-28T18:45:00-07:00",
      "error_message": "internal/syncer/syncer.go:4:1: package requires newer Go version go1.27 (application built with go1.26) - from `go run golang.org/x/vuln/cmd/govulncheck@v1.7.0 ./...` and `can't load config: the Go language version (go1.26) used to build golangci-lint is lower than the targeted Go version (1.27.0)` from `make lint`",
      "file": "Makefile",
      "root_cause": "`go run tool@version` builds the tool in the TOOL's module context, so the toolchain is selected from the tool's go.mod, not this module's. govulncheck v1.7.0 and golangci-lint v2.13.2 both declare go1.26, so both got built with go1.26 and their go/types could not parse a go1.27 module. Local-only: CI's golangci-lint-action downloads a prebuilt binary, and setup-go's environment builds govulncheck with 1.27, so CI on main was green throughout.",
      "fix": "Added GO_TOOLCHAIN = go$(shell go list -m -f '{{.GoVersion}}') to the Makefile (derived from go.mod, so it tracks upgrades) and prefixed the lint and vuln targets with GOTOOLCHAIN=$(GO_TOOLCHAIN). Both now pass locally.",
      "tags": [
        "go",
        "gotoolchain",
        "govulncheck",
        "golangci-lint",
        "makefile",
        "go1.27"
      ],
      "related_bugs": [
        "bug-006"
      ],
      "occurrences": 1,
      "last_seen": "2026-08-28T18:45:00-07:00"
    },
    {
      "id": "bug-008",
      "timestamp": "2026-08-28T18:45:00-07:00",
      "error_message": "ESLint first run: 'X' is defined but never used (NotificationBell.tsx); assertion unnecessary (store.tsx:433); Unexpected iterable of non-Promise values passed to promise aggregator (Macros.tsx:74)",
      "file": "web/src/components/triage/NotificationBell.tsx",
      "root_cause": "No JS/TS linter had ever run on the frontend, so dead imports, a no-op `as Enrichment` assertion, and a Promise.all fed an array padded with nulls all went unnoticed.",
      "fix": "Removed the unused lucide `X` import, dropped the redundant assertion, and rewrote the Macros reorder to flatMap so only real promises reach Promise.all. Also collapsed SecretField.source from '\"settings\" | \"env\" | string' (which TypeScript widens to plain string) to a documented string.",
      "tags": [
        "eslint",
        "frontend",
        "dead-code",
        "typescript",
        "promise"
      ],
      "related_bugs": [],
      "occurrences": 1,
      "last_seen": "2026-08-28T18:45:00-07:00"
    },
    {
      "id": "bug-009",
      "timestamp": "2026-08-28T19:05:00-07:00",
      "error_message": "CI 'Workflow lint' job failed: .github/workflows/release.yml:78:9: shellcheck reported issue in this script: SC2129:style: Consider using { cmd1; cmd2; } >> file instead of individual redirects [shellcheck]",
      "file": ".github/workflows/release.yml",
      "root_cause": "actionlint shells out to shellcheck for every `run:` block, but only if shellcheck is on PATH - otherwise it silently skips that check and still exits 0. GitHub's ubuntu runners ship shellcheck; this dev container did not. So the local actionlint run looked clean while CI failed on a pre-existing SC2129 in the 'Reproducible timestamps' step (three separate >> \"$GITHUB_ENV\" redirects). A green local actionlint proves much less than it appears when shellcheck is missing.",
      "fix": "Grouped the three echoes into a single { ... } >> \"$GITHUB_ENV\" redirect. Installed shellcheck locally and reproduced the failure before fixing. Added a guard to the Makefile actions-lint target: if shellcheck is absent it warns loudly that run: blocks are being skipped, and hard-fails when CI is set, so the blind spot cannot recur silently.",
      "tags": [
        "actionlint",
        "shellcheck",
        "github-actions",
        "ci",
        "silent-skip"
      ],
      "related_bugs": [
        "bug-010"
      ],
      "occurrences": 1,
      "last_seen": "2026-08-28T19:05:00-07:00"
    },
    {
      "id": "bug-010",
      "timestamp": "2026-08-28T19:05:00-07:00",
      "error_message": "fatal: no audit was performed / 'artipacked' audit failed ... couldn't list tags for actions/checkout ... HTTP status client error (401 Unauthorized) for url (https://github.com/actions/checkout.git/git-upload-pack)",
      "file": "Makefile",
      "root_cause": "zizmor runs online audits by default, which call the GitHub API. Unauthenticated, it does not degrade - it panics and exits non-zero, so no audit runs at all. Every earlier local run had used --no-online-audits, so the first invocation without it (via the new `make actions-lint`) crashed. The CI step as originally written also passed no token and would have hit the same 401 once actionlint stopped failing first.",
      "fix": "Pass --no-online-audits in the Makefile target that both CI and the pre-commit hook call. Deterministic, needs no token, identical locally and in CI; the online audits only check ref freshness, which Dependabot already covers. zizmor stays optional locally but the target hard-fails when CI is set, so a missing binary cannot silently skip the audit.",
      "tags": [
        "zizmor",
        "github-actions",
        "ci",
        "auth",
        "silent-skip"
      ],
      "related_bugs": [
        "bug-009"
      ],
      "occurrences": 1,
      "last_seen": "2026-08-28T19:05:00-07:00"
    },
    {
      "id": "bug-011",
      "timestamp": "2026-08-28T19:25:00-07:00",
      "error_message": "PR #16 stuck: required check 'Web typecheck and build' never reported ('Expected - waiting for status to be reported'), mergeable_state=blocked, while all 14 check runs on the head were green",
      "file": ".github/workflows/ci.yml",
      "root_cause": "The Main ruleset (id 21707865, active, strict) requires three status checks by exact name: golangci-lint, 'Web typecheck and build', 'Go fmt, vet, test'. This PR renamed the web job to 'Web lint, test, build' because it now lints and tests too. GitHub matches required checks by name, so the old context can never be satisfied and the PR blocks forever. Not a hung runner - the check was never going to run.",
      "fix": "Owner updates the ruleset to the current job names (and to require the new gates, which were not required at all: Workflow lint, Security, Dependency review, Analyze go, Analyze javascript-typescript, and the three 'Embed UI and compile binary (leg)' matrix names). Documented the trap in README's CI section so a future rename changes the ruleset in the same PR.",
      "tags": [
        "github-actions",
        "rulesets",
        "required-status-checks",
        "ci",
        "job-rename",
        "matrix"
      ],
      "related_bugs": [],
      "occurrences": 1,
      "last_seen": "2026-08-28T19:25:00-07:00"
    }
  ]
}
