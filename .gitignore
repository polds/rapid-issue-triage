/triage
*.db
*.db-shm
*.db-wal
node_modules/
.env
.env.*
.wolf/dashboard-token
.wolf/hooks/_*.json
.wolf/.honcho-sync-state.json
.wolf/_scan-state.json
.wolf/cron-state.json
.wolf/daemon.log
.wolf/token-ledger.json
/dist/
coverage.out

web/coverage/

# TypeScript's incremental build cache. Regenerated by `tsc`, machine-local,
# and referenced by nothing in the build - it only ever churns the diff.
web/tsconfig.tsbuildinfo

# Isolated git worktrees created for parallel agent runs; transient.
.claude/worktrees/
.run-sandbox/

# Scanner SARIF reports, written by `make sast` / `make osv` / `make trivy`
# and uploaded to the Security tab by CI.
semgrep.sarif
osv.sarif
trivy.sarif
